#This is a demo configuration file for EvoSuite
#DO NOT ADD COMMENTS IN THE SAME LINE AS WHERE YOU DEFINE A KEY=VALUE
#DO NOT ADD SPACES BETWEEN KEY AND =, = AND VALUE
#Arguments (criterion, seed, etc)

criterion=branch:weakmutation
seed=42

#Properties (search_budget, junit_suffix, etc)
#Important note: do not add the following properties, they are set within the script `Dtest_dir` and `Djunit_suffix`

Dsearch_budget=10
